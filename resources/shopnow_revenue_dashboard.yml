resources:
  dashboards:
    shopnow_revenue:
      display_name: "[${bundle.target}] ShopNow Revenue Intelligence"
      file_path: ../src/04-ai-bi/shopnow_revenue.lvdash.json
      warehouse_id: ${resources.sql_warehouses.shopnow_warehouse.id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
